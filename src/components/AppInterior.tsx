/** Calm work-mode backdrop — pattern only, no moving decoration. */
export default function AppInterior() {
  return (
    <div className="app-interior app-interior-calm" aria-hidden>
      <div className="app-interior-pattern" />
    </div>
  );
}
